- name: Update sshd config
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
  with_items:
    - { regexp: '^#?PublicKeyAuthentication\s+\w+', line: 'PublicKeyAuthentication yes' }
  notify: Restart sshd
